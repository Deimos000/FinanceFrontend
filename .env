EXPO_PUBLIC_API_URL=https://financebackend-331581307981.europe-west1.run.app

# Default redirect URL for banking (fallback if frontend doesn't send one)
# Ensure this URL is whitelisted in your Enable Banking Developer Console
BANKING_REDIRECT_URL=http://localhost:8081/accounts
